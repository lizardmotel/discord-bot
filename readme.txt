//install requirements
pip3 install -r requirements.txt -t Lib/site-packages

//branches
	//new branch
	git checkout -b [branch name]
	//change branch
	git checkout [branch name]

//git push to test branch
git add [file]
git commit -m [message]
push --set-upstream origin liz-test
